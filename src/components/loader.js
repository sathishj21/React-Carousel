import React from 'react';
import '../stlyesheet/loader.css';

const Loader = () => {
    return (
        <div className="loader">
            <div className="loader-inner"></div>
        </div>
    );
};

Loader.propTypes = {};
Loader.defaultProps = {};

export default Loader;
